<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pcban.omsap">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.C2D_MESSAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".LoginActivity" />
        <activity android:name=".sendingProposal.FileChooser" />
        <activity android:name=".sendingProposal.UploadToServer" />
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OrgProposals.ProposalDetails"
            android:label="@string/title_activity_proposal_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".OrgProfile.editProfile" />
        <activity android:name=".OrgProfile.ChangePassword" />
        <activity android:name=".TestingLayout" />
        <activity
            android:name=".ProfileScrollingActivity"
            android:label="@string/title_activity_profile_scrolling"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>